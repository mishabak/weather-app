import { Icon_paint } from "../icons";

function ThemeButton() {
  return (
    <div>
      <Icon_paint className={`cursor-pointer mr-5 hover:scale-105`} />
    </div>
  );
}

export default ThemeButton;
